<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - ChrisShop</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <div class="error-container p-5">
                    <i class="fas fa-exclamation-triangle fa-5x text-warning mb-4"></i>
                    <h1 class="fw-bold mb-3">Oops!</h1>
                    <h3 class="text-muted mb-4"><%= error || 'Ha ocurrido un error' %></h3>
                    <p class="text-muted mb-4">Lo sentimos, ha ocurrido un error inesperado.</p>
                    <a href="/" class="btn btn-primary me-2">
                        <i class="fas fa-home me-2"></i>Ir al Inicio
                    </a>
                    <a href="/products" class="btn btn-outline-primary">
                        <i class="fas fa-gamepad me-2"></i>Ver Productos
                    </a>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>